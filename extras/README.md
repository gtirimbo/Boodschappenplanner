# extras/

`boodschappenplanner.jsx` is the original React version of this app (built
for environments that run React artifacts, like Claude.ai). It's kept here
for reference — the file that actually gets deployed and shared is
`index.html` at the repo root, which has no build step or dependencies.
